# AIOps MVP Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build an exploratory AIOps prototype evaluating Drain3 + Keep + OpenAI GPT-5.4 + Grafana for automated root cause analysis.

**Architecture:** Single FastAPI monolith (`aiops-service`) with 5 external containers (Keep stack, ChromaDB, Grafana). Synchronous pipeline: ingest -> Drain -> anomaly detection -> Keep alerting. Separate AI investigation endpoint using OpenAI Responses API.

**Tech Stack:** Python 3.12, FastAPI, Drain3, scikit-learn, OpenAI SDK, ChromaDB, pandas, pydantic-settings, Docker Compose, Keep, Grafana.

**Spec:** `docs/superpowers/specs/2026-03-25-aiops-mvp-design.md`

---

## File Map

### Create (new files)

| File | Responsibility |
|------|---------------|
| `docker-compose.yml` | 6 containers orchestration |
| `.env.example` | Template with all env vars |
| `.gitignore` | Python + data + secrets |
| `services/aiops/Dockerfile` | Python 3.12 image for aiops-service |
| `services/aiops/requirements.txt` | All Python deps |
| `services/aiops/drain3.ini` | Drain3 config |
| `services/aiops/app/__init__.py` | Package init |
| `services/aiops/app/config.py` | pydantic-settings Settings class |
| `services/aiops/app/models.py` | Shared Pydantic models |
| `services/aiops/app/db.py` | SQLite init + helpers |
| `services/aiops/app/main.py` | FastAPI app with lifespan |
| `services/aiops/app/drain/__init__.py` | Package init |
| `services/aiops/app/drain/parser.py` | Drain3 TemplateMiner wrapper |
| `services/aiops/app/drain/anomaly.py` | IsolationForest anomaly detection |
| `services/aiops/app/drain/alerter.py` | Send alerts to Keep API |
| `services/aiops/app/agent/__init__.py` | Package init |
| `services/aiops/app/agent/investigator.py` | OpenAI Responses API agentic loop |
| `services/aiops/app/agent/tools.py` | 6 tool definitions + implementations |
| `services/aiops/app/agent/prompts.py` | System prompt with anti-failure rules |
| `services/aiops/app/agent/rag.py` | ChromaDB load + search |
| `services/aiops/app/adapters/__init__.py` | Package init |
| `services/aiops/app/adapters/openrca.py` | Read OpenRCA CSV files |
| `services/aiops/app/api/__init__.py` | Package init |
| `services/aiops/app/api/ingest.py` | POST /ingest/logs, /ingest/openrca |
| `services/aiops/app/api/clusters.py` | GET /clusters, /clusters/{id}, /clusters/timeline |
| `services/aiops/app/api/anomalies.py` | GET /anomalies |
| `services/aiops/app/api/investigate.py` | POST /investigate, GET /reports |
| `services/aiops/app/api/stats.py` | GET /stats |
| `services/aiops/app/api/benchmark.py` | POST /benchmark/run, GET /benchmark/results |
| `services/aiops/tests/__init__.py` | Package init |
| `services/aiops/tests/test_config.py` | Config loading tests |
| `services/aiops/tests/test_db.py` | Database schema tests |
| `services/aiops/tests/test_drain_parser.py` | Drain parser tests |
| `services/aiops/tests/test_anomaly.py` | Anomaly detection tests |
| `services/aiops/tests/test_openrca.py` | OpenRCA adapter tests |
| `services/aiops/tests/test_tools.py` | Agent tool tests |
| `services/aiops/tests/test_rag.py` | RAG module tests |
| `services/aiops/tests/test_investigator.py` | Investigator tests |
| `services/aiops/tests/test_api_ingest.py` | Ingest API tests |
| `services/aiops/tests/test_api_clusters.py` | Clusters API tests |
| `services/aiops/tests/test_api_investigate.py` | Investigate API tests |
| `scripts/setup_keep.py` | One-time Keep configuration |
| `services/aiops/knowledge/runbooks/cpu_high.md` | Sample runbook |
| `services/aiops/knowledge/runbooks/db_connection.md` | Sample runbook |
| `services/aiops/knowledge/runbooks/network_partition.md` | Sample runbook |
| `services/aiops/knowledge/past_incidents/inc_001.json` | Sample past incident |
| `grafana/provisioning/datasources/datasources.yml` | Grafana datasource config |
| `grafana/provisioning/dashboards/dashboards.yml` | Dashboard provider config |
| `grafana/provisioning/dashboards/log-clusters.json` | Log Clusters dashboard |
| `grafana/provisioning/dashboards/anomalies.json` | Anomalies dashboard |
| `grafana/provisioning/dashboards/incidents.json` | Incidents dashboard |
| `grafana/provisioning/dashboards/ai-reports.json` | AI Reports dashboard |
| `grafana/provisioning/dashboards/benchmark.json` | Benchmark dashboard |

---

## Parallel Execution Map

```
Batch 1 (no deps, parallel):
  Task 1: Project scaffolding
  Task 2: Docker Compose + Dockerfile

Batch 2 (depends on Task 1, parallel):
  Task 3: OpenRCA data adapter
  Task 4: Drain parser
  Task 5: RAG module

Batch 3 (mixed deps, parallel):
  Task 6: Anomaly detection      (depends on Task 4)
  Task 7: Agent tools             (depends on Task 3)

Batch 4 (mixed deps, parallel):
  Task 8: Keep alerter            (depends on Task 6)
  Task 9: Investigator            (depends on Task 5, 7)

Batch 5 (depends on Batch 1-4, parallel):
  Task 10: API routes - pipeline  (depends on Task 4, 6, 8)
  Task 11: API routes - agent     (depends on Task 9)
  Task 12: System prompt          (no code deps, can start anytime)

Batch 6 (depends on all, parallel):
  Task 13: FastAPI main.py        (depends on Task 10, 11)
  Task 14: Keep setup script      (depends on Task 2)
  Task 15: Grafana provisioning   (depends on Task 10, 11)

Batch 7 (final):
  Task 16: Integration test       (depends on all)
```

---

## Task 1: Project Scaffolding (config, models, db)

**Files:**
- Create: `services/aiops/app/__init__.py`
- Create: `services/aiops/app/config.py`
- Create: `services/aiops/app/models.py`
- Create: `services/aiops/app/db.py`
- Create: `services/aiops/requirements.txt`
- Create: `services/aiops/drain3.ini`
- Create: `.env.example`
- Create: `.gitignore`
- Test: `services/aiops/tests/test_config.py`
- Test: `services/aiops/tests/test_db.py`

- [ ] **Step 1: Create `.gitignore`**

```
.env
*.pyc
__pycache__/
data/
*.db
.venv/
```

- [ ] **Step 2: Create `.env.example`**

```
OPENAI_API_KEY=sk-your-key-here
KEEP_API_KEY=
```

- [ ] **Step 3: Create `requirements.txt`**

```
fastapi==0.115.*
uvicorn[standard]==0.34.*
drain3==0.9.*
scikit-learn==1.6.*
pandas==2.2.*
numpy==2.1.*
httpx==0.28.*
pydantic==2.10.*
pydantic-settings==2.7.*
openai==1.82.*
chromadb==0.6.*
pytest==8.3.*
pytest-asyncio==0.25.*
```

- [ ] **Step 4: Create `drain3.ini`**

```ini
[DRAIN]
sim_th = 0.4
depth = 4
max_children = 100
max_clusters = 1024
extra_delimiters = ["_"]
```

- [ ] **Step 5: Write failing test for config**

File: `services/aiops/tests/test_config.py`

```python
import os
import pytest
from app.config import Settings


def test_settings_defaults():
    os.environ["OPENAI_API_KEY"] = "test-key"
    s = Settings()
    assert s.OPENAI_MODEL == "gpt-5.4"
    assert s.KEEP_API_URL == "http://keep-api:8080"
    assert s.CHROMA_URL == "http://chromadb:8000"
    assert s.SQLITE_PATH == "/data/aiops.db"
    assert s.DRAIN_SIMILARITY == 0.4
    assert s.ANOMALY_WINDOW_SECONDS == 300


def test_settings_requires_api_key():
    os.environ.pop("OPENAI_API_KEY", None)
    with pytest.raises(Exception):
        Settings()
```

- [ ] **Step 6: Run test to verify it fails**

Run: `cd services/aiops && py -m pytest tests/test_config.py -v`
Expected: FAIL (module not found)

- [ ] **Step 7: Implement `config.py`**

```python
from pydantic_settings import BaseSettings


class Settings(BaseSettings):
    OPENAI_API_KEY: str
    OPENAI_MODEL: str = "gpt-5.4"
    AGENT_MAX_ITERATIONS: int = 20

    KEEP_API_URL: str = "http://keep-api:8080"
    KEEP_API_KEY: str = ""

    CHROMA_URL: str = "http://chromadb:8000"
    CHROMA_COLLECTION: str = "aiops_knowledge"
    EMBEDDING_MODEL: str = "text-embedding-3-small"

    OPENRCA_DATA_DIR: str = "/data/openrca"
    SQLITE_PATH: str = "/data/aiops.db"

    DRAIN_SIMILARITY: float = 0.4
    DRAIN_DEPTH: int = 4
    DRAIN_MAX_CLUSTERS: int = 1024

    ANOMALY_WINDOW_SECONDS: int = 300
    ANOMALY_CONTAMINATION: float = 0.1

    class Config:
        env_file = ".env"


settings = Settings()
```

- [ ] **Step 8: Run test to verify it passes**

Run: `cd services/aiops && py -m pytest tests/test_config.py -v`
Expected: PASS

- [ ] **Step 9: Write failing test for db**

File: `services/aiops/tests/test_db.py`

```python
import sqlite3
import tempfile
import os
import pytest
from app.db import init_db, get_db_path


def test_init_db_creates_tables():
    with tempfile.TemporaryDirectory() as tmpdir:
        db_path = os.path.join(tmpdir, "test.db")
        os.environ["SQLITE_PATH"] = db_path
        init_db(db_path)
        conn = sqlite3.connect(db_path)
        cursor = conn.execute(
            "SELECT name FROM sqlite_master WHERE type='table' ORDER BY name"
        )
        tables = [row[0] for row in cursor.fetchall()]
        conn.close()
        assert "log_entries" in tables
        assert "clusters" in tables
        assert "anomalies" in tables
        assert "reports" in tables
        assert "pending_alerts" in tables


def test_init_db_idempotent():
    with tempfile.TemporaryDirectory() as tmpdir:
        db_path = os.path.join(tmpdir, "test.db")
        init_db(db_path)
        init_db(db_path)  # should not raise
```

- [ ] **Step 10: Implement `db.py`**

```python
import sqlite3
from contextlib import contextmanager

_db_path: str = ""

SCHEMA = """
CREATE TABLE IF NOT EXISTS log_entries (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    timestamp TEXT NOT NULL,
    service TEXT NOT NULL,
    raw_message TEXT NOT NULL,
    cluster_id INTEGER NOT NULL,
    params TEXT,
    dataset TEXT NOT NULL,
    date TEXT NOT NULL
);

CREATE TABLE IF NOT EXISTS clusters (
    id INTEGER PRIMARY KEY,
    template TEXT NOT NULL,
    count INTEGER DEFAULT 0,
    first_seen TEXT,
    last_seen TEXT
);

CREATE TABLE IF NOT EXISTS anomalies (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    window_start TEXT NOT NULL,
    window_end TEXT NOT NULL,
    score REAL NOT NULL,
    anomaly_type TEXT NOT NULL,
    service TEXT,
    details TEXT,
    alert_sent INTEGER DEFAULT 0
);

CREATE TABLE IF NOT EXISTS reports (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    incident_id TEXT,
    dataset TEXT,
    date TEXT,
    created_at TEXT NOT NULL,
    root_cause TEXT NOT NULL,
    causal_chain TEXT,
    evidence TEXT,
    data_coverage TEXT,
    quality TEXT,
    correct INTEGER
);

CREATE TABLE IF NOT EXISTS pending_alerts (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    created_at TEXT NOT NULL,
    payload TEXT NOT NULL,
    retry_count INTEGER DEFAULT 0
);
"""


def get_db_path() -> str:
    return _db_path


def init_db(path: str) -> None:
    global _db_path
    _db_path = path
    conn = sqlite3.connect(path)
    conn.executescript(SCHEMA)
    conn.close()


@contextmanager
def get_connection():
    conn = sqlite3.connect(_db_path)
    conn.row_factory = sqlite3.Row
    try:
        yield conn
        conn.commit()
    finally:
        conn.close()
```

- [ ] **Step 11: Run db tests**

Run: `cd services/aiops && py -m pytest tests/test_db.py -v`
Expected: PASS

- [ ] **Step 12: Implement `models.py`**

```python
from pydantic import BaseModel
from typing import Optional


class LogEntry(BaseModel):
    timestamp: str
    service: str
    message: str


class ClusterInfo(BaseModel):
    id: int
    template: str
    count: int
    first_seen: Optional[str] = None
    last_seen: Optional[str] = None


class AnomalyInfo(BaseModel):
    id: int
    window_start: str
    window_end: str
    score: float
    anomaly_type: str
    service: Optional[str] = None
    details: Optional[dict] = None


class IngestRequest(BaseModel):
    dataset: str
    date: str


class InvestigateRequest(BaseModel):
    incident_id: str
    dataset: str
    date: str
    time_range: Optional[dict] = None


class RCAReport(BaseModel):
    id: int
    incident_id: Optional[str] = None
    created_at: str
    root_cause: dict
    causal_chain: list
    evidence: list
    data_coverage: dict
    quality: dict
    correct: Optional[int] = None


class StatsResponse(BaseModel):
    total_logs: int
    unique_templates: int
    anomaly_count: int
    anomaly_rate: float
    last_ingest: Optional[str] = None
```

- [ ] **Step 13: Commit**

```bash
git add -A && git commit -m "feat: project scaffolding - config, models, db, requirements"
```

---

## Task 2: Docker Compose + Dockerfile

**Files:**
- Create: `docker-compose.yml`
- Create: `services/aiops/Dockerfile`

- [ ] **Step 1: Create `Dockerfile`**

```dockerfile
FROM python:3.12-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY drain3.ini .
COPY app/ app/
COPY knowledge/ knowledge/

EXPOSE 8081

CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8081"]
```

- [ ] **Step 2: Create `docker-compose.yml`**

```yaml
services:
  aiops-service:
    build: ./services/aiops
    ports:
      - "8081:8081"
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - KEEP_API_URL=http://keep-api:8080
      - KEEP_API_KEY=${KEEP_API_KEY}
      - CHROMA_URL=http://chromadb:8000
      - OPENRCA_DATA_DIR=/data/openrca
      - SQLITE_PATH=/data/aiops.db
    volumes:
      - ./data:/data
      - ./services/aiops/knowledge:/app/knowledge
    depends_on:
      chromadb:
        condition: service_healthy
      keep-api:
        condition: service_started

  keep-api:
    image: us-central1-docker.pkg.dev/keephq/keep/keep-api:latest
    ports:
      - "8080:8080"
    environment:
      - DATABASE_URL=postgresql://keep:keep@keep-db:5432/keep
      - SECRET_KEY=dev-secret-key-change-in-prod
      - KEEP_TOPOLOGY_PROCESSOR=true
      - KEEP_TOPOLOGY_PROCESSOR_INTERVAL=10
    depends_on:
      keep-db:
        condition: service_healthy

  keep-ui:
    image: us-central1-docker.pkg.dev/keephq/keep/keep-ui:latest
    ports:
      - "3001:3000"
    environment:
      - API_URL=http://keep-api:8080
      - NEXTAUTH_SECRET=dev-secret

  keep-db:
    image: postgres:15
    environment:
      - POSTGRES_USER=keep
      - POSTGRES_PASSWORD=keep
      - POSTGRES_DB=keep
    volumes:
      - keep-db-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U keep"]
      interval: 5s
      timeout: 5s
      retries: 5

  chromadb:
    image: chromadb/chroma:latest
    ports:
      - "8000:8000"
    volumes:
      - chroma-data:/chroma/chroma
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/api/v1/heartbeat"]
      interval: 5s
      timeout: 5s
      retries: 5

  grafana:
    image: grafana/grafana-oss:latest
    ports:
      - "3000:3000"
    environment:
      - GF_SECURITY_ADMIN_PASSWORD=admin
      - GF_INSTALL_PLUGINS=yesoreyeram-infinity-datasource
    volumes:
      - ./grafana/provisioning:/etc/grafana/provisioning

volumes:
  keep-db-data:
  chroma-data:
```

- [ ] **Step 3: Verify Docker Compose syntax**

Run: `docker compose config --quiet`
Expected: No errors

- [ ] **Step 4: Commit**

```bash
git add docker-compose.yml services/aiops/Dockerfile && git commit -m "feat: Docker Compose with 6 containers + Dockerfile"
```

---

## Task 3: OpenRCA Data Adapter

**Files:**
- Create: `services/aiops/app/adapters/__init__.py`
- Create: `services/aiops/app/adapters/openrca.py`
- Test: `services/aiops/tests/test_openrca.py`

- [ ] **Step 1: Create test fixtures -- sample CSV files**

File: `services/aiops/tests/conftest.py`

```python
import os
import pytest
import tempfile


@pytest.fixture
def sample_openrca_dir():
    with tempfile.TemporaryDirectory() as tmpdir:
        base = os.path.join(tmpdir, "Bank", "2024_01_15")
        log_dir = os.path.join(base, "log")
        metric_dir = os.path.join(base, "metric")
        trace_dir = os.path.join(base, "trace")
        os.makedirs(log_dir)
        os.makedirs(metric_dir)
        os.makedirs(trace_dir)

        # Sample log CSV
        with open(os.path.join(log_dir, "gateway.csv"), "w") as f:
            f.write("timestamp,message\n")
            f.write("2024-01-15 10:10:00,Connection timeout to db-master after 30s\n")
            f.write("2024-01-15 10:10:01,Request processed in 120ms\n")
            f.write("2024-01-15 10:10:02,Health check OK\n")

        # Sample metric CSV
        with open(os.path.join(metric_dir, "gateway.csv"), "w") as f:
            f.write("timestamp,metric_name,value\n")
            f.write("2024-01-15 10:10:00,cpu,0.45\n")
            f.write("2024-01-15 10:10:00,latency_p99,120\n")
            f.write("2024-01-15 10:15:00,cpu,0.92\n")
            f.write("2024-01-15 10:15:00,latency_p99,4200\n")

        # Sample trace CSV
        with open(os.path.join(trace_dir, "traces.csv"), "w") as f:
            f.write("trace_id,span_id,parent_span_id,service,operation,duration_ms,status\n")
            f.write("t1,s1,,gateway,handle_request,4100,error\n")
            f.write("t1,s2,s1,payment-api,process_payment,4050,error\n")
            f.write("t1,s3,s2,db-master,query,4000,timeout\n")

        # Sample record.csv (ground truth)
        with open(os.path.join(base, "record.csv"), "w") as f:
            f.write("root_cause,component,description\n")
            f.write("db_lock_contention,db-master,Lock contention from unindexed query\n")

        yield tmpdir
```

- [ ] **Step 2: Write failing tests for OpenRCA adapter**

File: `services/aiops/tests/test_openrca.py`

```python
from app.adapters.openrca import OpenRCAAdapter


def test_load_logs(sample_openrca_dir):
    adapter = OpenRCAAdapter(sample_openrca_dir)
    logs = adapter.load_logs("Bank", "2024_01_15")
    assert len(logs) == 3
    assert logs[0].service == "gateway"
    assert "Connection timeout" in logs[0].message


def test_load_logs_by_service(sample_openrca_dir):
    adapter = OpenRCAAdapter(sample_openrca_dir)
    logs = adapter.load_logs("Bank", "2024_01_15", service="gateway")
    assert len(logs) == 3


def test_load_metrics(sample_openrca_dir):
    adapter = OpenRCAAdapter(sample_openrca_dir)
    result = adapter.load_metrics("Bank", "2024_01_15", service="gateway")
    assert "anomalies" in result or "metrics" in result


def test_load_traces(sample_openrca_dir):
    adapter = OpenRCAAdapter(sample_openrca_dir)
    result = adapter.load_traces("Bank", "2024_01_15")
    assert "critical_path" in result
    assert result["bottleneck"] == "db-master"


def test_load_topology(sample_openrca_dir):
    adapter = OpenRCAAdapter(sample_openrca_dir)
    topo = adapter.load_topology("Bank")
    assert "nodes" in topo
    assert "edges" in topo


def test_load_ground_truth(sample_openrca_dir):
    adapter = OpenRCAAdapter(sample_openrca_dir)
    gt = adapter.load_ground_truth("Bank", "2024_01_15")
    assert gt["component"] == "db-master"
```

- [ ] **Step 3: Run tests to verify they fail**

Run: `cd services/aiops && py -m pytest tests/test_openrca.py -v`
Expected: FAIL (module not found)

- [ ] **Step 4: Implement `openrca.py`**

Implement `OpenRCAAdapter` class with methods:
- `load_logs(dataset, date, service=None)` -- reads log/*.csv via pandas, returns List[LogEntry]
- `load_metrics(dataset, date, service, metric=None, time_range=None)` -- reads metric/*.csv, computes baseline (mean of first half) + deviation, returns dict with anomalies list
- `load_traces(dataset, date, time_range=None)` -- reads trace/*.csv, builds critical path by following parent_span_id chain, identifies bottleneck (longest span)
- `load_topology(dataset)` -- parses trace data to build service dependency graph (nodes + edges)
- `load_ground_truth(dataset, date)` -- reads record.csv, returns dict with root cause info

Key implementation details:
- Use `functools.lru_cache` or internal dict for caching loaded DataFrames
- `load_metrics` returns structured dict: `{"service": "...", "anomalies": [...], "normal_metrics": [...], "earliest_anomaly": "..."}`
- `load_traces` returns: `{"critical_path": [...], "bottleneck": "...", "anomalous_spans_count": N}`

- [ ] **Step 5: Run tests to verify they pass**

Run: `cd services/aiops && py -m pytest tests/test_openrca.py -v`
Expected: PASS

- [ ] **Step 6: Commit**

```bash
git add services/aiops/app/adapters/ services/aiops/tests/test_openrca.py services/aiops/tests/conftest.py
git commit -m "feat: OpenRCA data adapter with caching and preprocessing"
```

---

## Task 4: Drain Parser

**Files:**
- Create: `services/aiops/app/drain/__init__.py`
- Create: `services/aiops/app/drain/parser.py`
- Test: `services/aiops/tests/test_drain_parser.py`

- [ ] **Step 1: Write failing tests**

File: `services/aiops/tests/test_drain_parser.py`

```python
from app.drain.parser import DrainParser


def test_parse_single_log():
    parser = DrainParser()
    result = parser.parse("Connection timeout to db-master after 30s")
    assert result["cluster_id"] is not None
    assert result["template"] is not None


def test_parse_groups_similar_logs():
    parser = DrainParser()
    r1 = parser.parse("Connection timeout to db-master after 30s")
    r2 = parser.parse("Connection timeout to payment-api after 45s")
    assert r1["cluster_id"] == r2["cluster_id"]


def test_get_clusters():
    parser = DrainParser()
    parser.parse("Connection timeout to db-master after 30s")
    parser.parse("Health check OK")
    clusters = parser.get_clusters()
    assert len(clusters) == 2


def test_batch_parse():
    parser = DrainParser()
    lines = [
        "Connection timeout to db-master after 30s",
        "Connection timeout to payment-api after 45s",
        "Health check OK",
    ]
    results = parser.batch_parse(lines)
    assert len(results) == 3
```

- [ ] **Step 2: Run to verify failure**

Run: `cd services/aiops && py -m pytest tests/test_drain_parser.py -v`
Expected: FAIL

- [ ] **Step 3: Implement `parser.py`**

```python
from drain3 import TemplateMiner
from drain3.template_miner_config import TemplateMinerConfig
from typing import Optional
import os


class DrainParser:
    def __init__(self, config_path: Optional[str] = None):
        config = TemplateMinerConfig()
        if config_path and os.path.exists(config_path):
            config.load(config_path)
        self.miner = TemplateMiner(config=config)

    def parse(self, log_line: str) -> dict:
        result = self.miner.add_log_message(log_line)
        return {
            "cluster_id": result.cluster_id,
            "template": result.template_mined,
            "change_type": result.change_type,
        }

    def batch_parse(self, lines: list[str]) -> list[dict]:
        return [self.parse(line) for line in lines]

    def get_clusters(self) -> list[dict]:
        clusters = []
        for cluster in self.miner.drain.clusters:
            clusters.append({
                "id": cluster.cluster_id,
                "template": cluster.get_template(),
                "count": cluster.size,
            })
        return clusters

    def get_cluster(self, cluster_id: int) -> Optional[dict]:
        for c in self.miner.drain.clusters:
            if c.cluster_id == cluster_id:
                return {
                    "id": c.cluster_id,
                    "template": c.get_template(),
                    "count": c.size,
                }
        return None
```

- [ ] **Step 4: Run tests**

Run: `cd services/aiops && py -m pytest tests/test_drain_parser.py -v`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add services/aiops/app/drain/ services/aiops/tests/test_drain_parser.py
git commit -m "feat: Drain3 log parser with batch processing"
```

---

## Task 5: RAG Module (ChromaDB)

**Files:**
- Create: `services/aiops/app/agent/__init__.py`
- Create: `services/aiops/app/agent/rag.py`
- Create: `services/aiops/knowledge/runbooks/cpu_high.md`
- Create: `services/aiops/knowledge/runbooks/db_connection.md`
- Create: `services/aiops/knowledge/runbooks/network_partition.md`
- Create: `services/aiops/knowledge/past_incidents/inc_001.json`
- Test: `services/aiops/tests/test_rag.py`

- [ ] **Step 1: Create sample knowledge files**

`knowledge/runbooks/cpu_high.md`:
```markdown
# CPU High Alert Runbook

CPU spikes are almost always a SYMPTOM, not a root cause.

## Investigation Steps
1. Check which process is consuming CPU (top/htop)
2. Check if there was a recent deployment
3. Check database query latency - unindexed queries cause CPU spikes
4. Check for connection pool exhaustion upstream
5. Check memory - GC pressure causes CPU spikes

## Common Root Causes
- Unindexed database query after schema migration
- Connection pool exhaustion causing retry storms
- Memory leak triggering aggressive garbage collection
```

`knowledge/runbooks/db_connection.md`:
```markdown
# Database Connection Issues Runbook

## Symptoms
- Connection timeout errors in application logs
- Connection pool exhausted warnings
- Increased latency across dependent services

## Investigation Steps
1. Check db connection count vs pool limit
2. Check for long-running queries (locks)
3. Check if max_connections was recently changed
4. Trace upstream: what is generating excess connections?

## Common Root Causes
- Long-running transactions holding locks
- Missing index causing table scans
- Connection leak in application code
```

`knowledge/runbooks/network_partition.md`:
```markdown
# Network Partition Runbook

## Symptoms
- Intermittent connection timeouts between services
- Increased latency on cross-service calls
- Some requests succeed, others fail

## Investigation Steps
1. Check trace data for inter-service latency spikes
2. Compare affected vs unaffected service pairs
3. Check if affected services share a network segment
4. Look for DNS resolution failures

## Common Root Causes
- Switch/router failure in specific rack
- DNS misconfiguration after deployment
- Security group / firewall rule change
```

`knowledge/past_incidents/inc_001.json`:
```json
{
    "title": "Payment API CPU spike caused by unindexed query",
    "root_cause": "Missing index on orders.customer_id after migration v2.4.1",
    "symptoms": ["CPU > 95%", "Latency p99 > 5s", "503 errors"],
    "resolution": "Added index, rolled back to v2.3.0",
    "affected_services": ["payment-api", "db-master"]
}
```

- [ ] **Step 2: Write failing tests for RAG**

File: `services/aiops/tests/test_rag.py`

```python
import pytest
from unittest.mock import MagicMock, patch


def test_load_knowledge_files():
    from app.agent.rag import RAGManager
    rag = RAGManager.__new__(RAGManager)
    rag.knowledge_dir = "services/aiops/knowledge"
    docs = rag._read_knowledge_files()
    assert len(docs) >= 4  # 3 runbooks + 1 incident


def test_search_returns_results():
    # Integration test -- requires ChromaDB or mock
    from app.agent.rag import RAGManager
    with patch("app.agent.rag.chromadb") as mock_chroma:
        mock_collection = MagicMock()
        mock_collection.query.return_value = {
            "documents": [["CPU high runbook content"]],
            "metadatas": [[{"source": "cpu_high.md"}]],
            "distances": [[0.3]],
        }
        mock_client = MagicMock()
        mock_client.get_or_create_collection.return_value = mock_collection
        mock_chroma.HttpClient.return_value = mock_client

        rag = RAGManager(
            chroma_url="http://localhost:8000",
            collection_name="test",
            knowledge_dir="services/aiops/knowledge",
            openai_api_key="test-key",
        )
        results = rag.search("CPU spike investigation")
        assert len(results) > 0
```

- [ ] **Step 3: Implement `rag.py`**

Implement `RAGManager` class:
- `__init__(chroma_url, collection_name, knowledge_dir, openai_api_key)` -- connect to ChromaDB
- `load_knowledge()` -- read runbooks + incidents from knowledge dir, embed via OpenAI, upsert into collection
- `search(query, top_k=3)` -- embed query, search collection, return results
- `_read_knowledge_files()` -- scan knowledge dir for .md and .json files
- `_embed(texts)` -- call OpenAI embeddings API (`text-embedding-3-small`)

- [ ] **Step 4: Run tests**

Run: `cd services/aiops && py -m pytest tests/test_rag.py -v`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add services/aiops/app/agent/ services/aiops/knowledge/ services/aiops/tests/test_rag.py
git commit -m "feat: RAG module with ChromaDB and sample knowledge base"
```

---

## Task 6: Anomaly Detection

**Files:**
- Create: `services/aiops/app/drain/anomaly.py`
- Test: `services/aiops/tests/test_anomaly.py`

Depends on: Task 4 (Drain parser)

- [ ] **Step 1: Write failing tests**

File: `services/aiops/tests/test_anomaly.py`

```python
import numpy as np
from app.drain.anomaly import AnomalyDetector


def test_detect_normal_windows():
    detector = AnomalyDetector(window_seconds=300, contamination=0.1)
    # 10 normal windows with steady template counts
    normal_data = [
        {"window_start": f"2024-01-15T10:{i*5:02d}:00Z",
         "template_counts": {1: 100, 2: 50, 3: 20}}
        for i in range(10)
    ]
    anomalies = detector.detect(normal_data)
    assert len(anomalies) <= 1  # at most 1 false positive from IsolationForest


def test_detect_anomalous_window():
    detector = AnomalyDetector(window_seconds=300, contamination=0.1)
    windows = [
        {"window_start": f"2024-01-15T10:{i*5:02d}:00Z",
         "template_counts": {1: 100, 2: 50}}
        for i in range(9)
    ]
    # Anomalous window: template 1 spikes 50x
    windows.append({
        "window_start": "2024-01-15T10:45:00Z",
        "template_counts": {1: 5000, 2: 50}
    })
    anomalies = detector.detect(windows)
    assert len(anomalies) >= 1
    assert anomalies[0]["window_start"] == "2024-01-15T10:45:00Z"


def test_detect_new_template():
    detector = AnomalyDetector(window_seconds=300, contamination=0.1)
    anomalies = detector.detect_new_templates(
        known_templates={1, 2, 3},
        current_templates={1, 2, 3, 99}
    )
    assert 99 in [a["cluster_id"] for a in anomalies]
```

- [ ] **Step 2: Implement `anomaly.py`**

Implement `AnomalyDetector`:
- `detect(windows)` -- build count matrix, run IsolationForest, return list of anomalous windows with scores and contributing templates
- `detect_new_templates(known, current)` -- flag new template IDs
- `build_count_matrix(windows)` -- windows x templates numpy matrix

- [ ] **Step 3: Run tests**

Run: `cd services/aiops && py -m pytest tests/test_anomaly.py -v`
Expected: PASS

- [ ] **Step 4: Commit**

```bash
git add services/aiops/app/drain/anomaly.py services/aiops/tests/test_anomaly.py
git commit -m "feat: anomaly detection with IsolationForest + new template detection"
```

---

## Task 7: Agent Tools

**Files:**
- Create: `services/aiops/app/agent/tools.py`
- Test: `services/aiops/tests/test_tools.py`

Depends on: Task 3 (OpenRCA adapter)

- [ ] **Step 1: Write failing tests**

File: `services/aiops/tests/test_tools.py`

```python
from app.agent.tools import TOOL_DEFINITIONS, get_tool_registry


def test_tool_definitions_valid():
    assert len(TOOL_DEFINITIONS) == 6
    for tool in TOOL_DEFINITIONS:
        assert tool["type"] == "function"
        assert "name" in tool
        assert "parameters" in tool
        assert tool["parameters"].get("additionalProperties") is False


def test_tool_registry_has_all_tools():
    # Mock dependencies
    registry = get_tool_registry(
        openrca_adapter=None,
        db_path=None,
        rag_manager=None,
    )
    expected = {"query_metrics", "query_logs", "query_traces",
                "get_topology", "get_recent_changes", "search_knowledge_base"}
    assert set(registry.keys()) == expected
```

- [ ] **Step 2: Implement `tools.py`**

Implement:
- `TOOL_DEFINITIONS` -- list of 6 tool dicts in OpenAI Responses API format (flat, strict, additionalProperties: False)
- `get_tool_registry(openrca_adapter, db_path, rag_manager)` -- returns dict mapping tool name to callable
- Each tool callable receives kwargs and returns dict (compressed summary, not raw data)
- `query_metrics` calls `openrca_adapter.load_metrics()`, formats as anomaly summary
- `query_logs` queries SQLite clusters table, computes baseline deviation
- `query_traces` calls `openrca_adapter.load_traces()`
- `get_topology` calls `openrca_adapter.load_topology()`
- `get_recent_changes` calls `openrca_adapter.load_ground_truth()`
- `search_knowledge_base` calls `rag_manager.search()`

- [ ] **Step 3: Run tests**

Run: `cd services/aiops && py -m pytest tests/test_tools.py -v`
Expected: PASS

- [ ] **Step 4: Commit**

```bash
git add services/aiops/app/agent/tools.py services/aiops/tests/test_tools.py
git commit -m "feat: 6 agent tools with compressed summary returns"
```

---

## Task 8: Keep Alerter

**Files:**
- Create: `services/aiops/app/drain/alerter.py`
- Test: `services/aiops/tests/test_alerter.py`

Depends on: Task 6 (Anomaly detection)

- [ ] **Step 1: Write failing tests**

File: `services/aiops/tests/test_alerter.py`

```python
import json
import pytest
from unittest.mock import AsyncMock, patch, MagicMock
from app.drain.alerter import KeepAlerter


@pytest.mark.asyncio
async def test_send_alert_success():
    with patch("app.drain.alerter.httpx.AsyncClient") as mock_client_cls:
        mock_client = AsyncMock()
        mock_client.post.return_value = MagicMock(status_code=200)
        mock_client_cls.return_value.__aenter__ = AsyncMock(return_value=mock_client)
        mock_client_cls.return_value.__aexit__ = AsyncMock(return_value=False)

        alerter = KeepAlerter("http://keep:8080", "test-key", db_path=":memory:")
        await alerter.send_alert({
            "service": "gateway",
            "template": "Connection timeout to <*>",
            "score": 0.95,
            "anomaly_type": "isolation_forest",
        })
        mock_client.post.assert_called_once()


@pytest.mark.asyncio
async def test_send_alert_queues_on_failure():
    with patch("app.drain.alerter.httpx.AsyncClient") as mock_client_cls:
        mock_client = AsyncMock()
        mock_client.post.side_effect = Exception("Connection refused")
        mock_client_cls.return_value.__aenter__ = AsyncMock(return_value=mock_client)
        mock_client_cls.return_value.__aexit__ = AsyncMock(return_value=False)

        alerter = KeepAlerter("http://keep:8080", "test-key", db_path=":memory:")
        alerter._init_pending_table()
        await alerter.send_alert({
            "service": "gateway",
            "template": "test",
            "score": 0.9,
            "anomaly_type": "test",
        })
        # Alert should be queued in pending_alerts
        pending = alerter.get_pending_count()
        assert pending >= 1
```

- [ ] **Step 2: Implement `alerter.py`**

Implement `KeepAlerter`:
- `send_alert(anomaly_dict)` -- POST to Keep API, queue in SQLite on failure
- `retry_pending()` -- retry queued alerts
- `get_pending_count()` -- count pending alerts

- [ ] **Step 3: Run tests**

Run: `cd services/aiops && py -m pytest tests/test_alerter.py -v`
Expected: PASS

- [ ] **Step 4: Commit**

```bash
git add services/aiops/app/drain/alerter.py services/aiops/tests/test_alerter.py
git commit -m "feat: Keep alerter with retry queue on failure"
```

---

## Task 9: AI Investigator

**Files:**
- Create: `services/aiops/app/agent/investigator.py`
- Test: `services/aiops/tests/test_investigator.py`

Depends on: Task 5 (RAG), Task 7 (Agent tools)

- [ ] **Step 1: Write failing tests**

File: `services/aiops/tests/test_investigator.py`

```python
import json
import pytest
from unittest.mock import MagicMock, patch


def test_investigate_returns_report():
    from app.agent.investigator import Investigator

    # Mock OpenAI response -- model returns final answer (no tool calls)
    mock_response = MagicMock()
    mock_output_item = MagicMock()
    mock_output_item.type = "text"
    mock_response.output = [mock_output_item]
    mock_response.output_text = json.dumps({
        "root_cause": {"component": "db-master", "reason": "test", "onset_time": "...", "confidence": 0.9},
        "causal_chain": ["db-master -> payment-api"],
        "evidence": ["test evidence"],
        "data_coverage": {"metrics_checked": [], "logs_checked": [], "traces_checked": []},
        "investigation_quality": {"total_tool_calls": 0, "all_data_types_checked": False, "upstream_followed": False},
    })

    with patch("app.agent.investigator.OpenAI") as MockOpenAI:
        mock_client = MagicMock()
        mock_client.responses.create.return_value = mock_response
        MockOpenAI.return_value = mock_client

        investigator = Investigator(
            api_key="test-key",
            model="gpt-5.4",
            tool_registry={},
            tool_definitions=[],
            max_iterations=5,
        )
        report = investigator.investigate("Test incident context")
        assert report["root_cause"]["component"] == "db-master"


def test_investigate_executes_tool_calls():
    from app.agent.investigator import Investigator

    # First response: model calls a tool
    tool_call = MagicMock()
    tool_call.type = "function_call"
    tool_call.name = "get_topology"
    tool_call.arguments = '{"dataset": "Bank"}'
    tool_call.call_id = "call_1"

    response1 = MagicMock()
    response1.output = [tool_call]

    # Second response: model returns final answer
    response2 = MagicMock()
    text_item = MagicMock()
    text_item.type = "text"
    response2.output = [text_item]
    response2.output_text = json.dumps({
        "root_cause": {"component": "db-master", "reason": "test", "onset_time": "...", "confidence": 0.9},
        "causal_chain": [],
        "evidence": [],
        "data_coverage": {"metrics_checked": [], "logs_checked": [], "traces_checked": []},
        "investigation_quality": {"total_tool_calls": 1, "all_data_types_checked": False, "upstream_followed": False},
    })

    with patch("app.agent.investigator.OpenAI") as MockOpenAI:
        mock_client = MagicMock()
        mock_client.responses.create.side_effect = [response1, response2]
        MockOpenAI.return_value = mock_client

        mock_tool = MagicMock(return_value={"nodes": ["gateway"], "edges": []})

        investigator = Investigator(
            api_key="test-key",
            model="gpt-5.4",
            tool_registry={"get_topology": mock_tool},
            tool_definitions=[],
            max_iterations=5,
        )
        report = investigator.investigate("Test incident")
        mock_tool.assert_called_once()
        assert report is not None
```

- [ ] **Step 2: Implement `investigator.py`**

Implement `Investigator` class with agentic loop per spec (Section 5):
- `__init__(api_key, model, tool_registry, tool_definitions, max_iterations)` -- create OpenAI client
- `investigate(context: str) -> dict` -- the main loop:
  1. Build input_list with user message
  2. Call `client.responses.create()` with tools
  3. If function_calls in output: execute tools, append results, loop
  4. If no function_calls: parse output_text as JSON, return report
  5. Retry on OpenAI errors (3x exponential backoff)

- [ ] **Step 3: Run tests**

Run: `cd services/aiops && py -m pytest tests/test_investigator.py -v`
Expected: PASS

- [ ] **Step 4: Commit**

```bash
git add services/aiops/app/agent/investigator.py services/aiops/tests/test_investigator.py
git commit -m "feat: AI investigator with OpenAI Responses API agentic loop"
```

---

## Task 10: API Routes -- Pipeline (ingest, clusters, anomalies, stats)

**Files:**
- Create: `services/aiops/app/api/__init__.py`
- Create: `services/aiops/app/api/ingest.py`
- Create: `services/aiops/app/api/clusters.py`
- Create: `services/aiops/app/api/anomalies.py`
- Create: `services/aiops/app/api/stats.py`
- Test: `services/aiops/tests/test_api_ingest.py`
- Test: `services/aiops/tests/test_api_clusters.py`

Depends on: Task 4 (Drain), Task 6 (Anomaly), Task 8 (Alerter)

- [ ] **Step 1: Write failing API tests**

File: `services/aiops/tests/test_api_ingest.py`

```python
from fastapi.testclient import TestClient
from unittest.mock import patch, MagicMock


def test_ingest_openrca_endpoint():
    # Will test once main.py is created (Task 13)
    # For now, test the route function directly
    from app.api.ingest import router
    from fastapi import FastAPI

    app = FastAPI()
    app.include_router(router)
    # Store dependencies on app.state for testing
    # Actual integration test in Task 16
```

File: `services/aiops/tests/test_api_clusters.py`

```python
def test_clusters_route_exists():
    from app.api.clusters import router
    routes = [r.path for r in router.routes]
    assert "/clusters" in routes or any("/clusters" in r for r in routes)
```

- [ ] **Step 2: Implement API route files**

Each file creates an `APIRouter` with endpoints:

`api/ingest.py`:
- `POST /ingest/openrca` -- accepts IngestRequest, calls OpenRCA adapter -> Drain -> anomaly detection -> Keep alerter
- `POST /ingest/logs` -- accepts raw log lines, processes through Drain

`api/clusters.py`:
- `GET /clusters` -- returns all Drain clusters from SQLite
- `GET /clusters/{cluster_id}` -- returns single cluster detail
- `GET /clusters/timeline` -- returns template counts per time window

`api/anomalies.py`:
- `GET /anomalies` -- returns all anomalies from SQLite

`api/stats.py`:
- `GET /stats` -- returns StatsResponse with aggregate counts

All routes access shared state via `request.app.state` (Drain parser, OpenRCA adapter, db_path, etc.)

- [ ] **Step 3: Run tests**

Run: `cd services/aiops && py -m pytest tests/test_api_clusters.py tests/test_api_ingest.py -v`
Expected: PASS

- [ ] **Step 4: Commit**

```bash
git add services/aiops/app/api/ services/aiops/tests/test_api_*.py
git commit -m "feat: API routes for ingest, clusters, anomalies, stats"
```

---

## Task 11: API Routes -- Agent (investigate, reports, benchmark)

**Files:**
- Create: `services/aiops/app/api/investigate.py`
- Create: `services/aiops/app/api/benchmark.py`
- Test: `services/aiops/tests/test_api_investigate.py`

Depends on: Task 9 (Investigator)

- [ ] **Step 1: Implement `api/investigate.py`**

- `POST /investigate` -- accepts InvestigateRequest, runs Investigator, saves report to SQLite, returns RCAReport
- `GET /reports` -- returns all reports from SQLite
- `GET /reports/{report_id}` -- returns single report

- [ ] **Step 2: Implement `api/benchmark.py`**

- `POST /benchmark/run` -- accepts `{dataset, dates}`, for each date: runs full pipeline (ingest -> drain -> anomaly -> keep -> investigate), compares with ground truth, saves results
- `GET /benchmark/results` -- returns accuracy metrics

- [ ] **Step 3: Write tests**

File: `services/aiops/tests/test_api_investigate.py`

```python
def test_investigate_route_exists():
    from app.api.investigate import router
    paths = [r.path for r in router.routes]
    assert any("investigate" in p for p in paths)


def test_reports_route_exists():
    from app.api.investigate import router
    paths = [r.path for r in router.routes]
    assert any("reports" in p for p in paths)
```

- [ ] **Step 4: Run tests**

Run: `cd services/aiops && py -m pytest tests/test_api_investigate.py -v`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add services/aiops/app/api/investigate.py services/aiops/app/api/benchmark.py services/aiops/tests/test_api_investigate.py
git commit -m "feat: API routes for investigate, reports, benchmark"
```

---

## Task 12: System Prompt

**Files:**
- Create: `services/aiops/app/agent/prompts.py`

No dependencies, can run anytime.

- [ ] **Step 1: Implement `prompts.py`**

Implement `SYSTEM_PROMPT` string with:
- Mandatory investigation protocol (3 phases: overview, causal analysis, verification)
- Anti-failure-mode rules (5 rules from spec Section 5)
- Structured JSON output format
- `{incident_context}` placeholder

See spec Section 5 for exact rules.

- [ ] **Step 2: Commit**

```bash
git add services/aiops/app/agent/prompts.py
git commit -m "feat: system prompt with 3-phase investigation and anti-failure rules"
```

---

## Task 13: FastAPI Main App

**Files:**
- Create: `services/aiops/app/main.py`

Depends on: Task 10, Task 11 (all API routes)

- [ ] **Step 1: Implement `main.py`**

```python
from contextlib import asynccontextmanager
from fastapi import FastAPI
from fastapi.middleware.cors import CORSMiddleware
from app.config import settings
from app.db import init_db
from app.drain.parser import DrainParser
from app.adapters.openrca import OpenRCAAdapter
from app.agent.rag import RAGManager
from app.api import ingest, clusters, anomalies, stats, investigate, benchmark


@asynccontextmanager
async def lifespan(app: FastAPI):
    # Startup
    init_db(settings.SQLITE_PATH)
    app.state.drain_parser = DrainParser(config_path="drain3.ini")
    app.state.openrca = OpenRCAAdapter(settings.OPENRCA_DATA_DIR)
    app.state.rag = RAGManager(
        chroma_url=settings.CHROMA_URL,
        collection_name=settings.CHROMA_COLLECTION,
        knowledge_dir="knowledge",
        openai_api_key=settings.OPENAI_API_KEY,
    )
    app.state.rag.load_knowledge()
    yield
    # Shutdown (cleanup if needed)


app = FastAPI(title="AIOps Service", lifespan=lifespan)

app.add_middleware(
    CORSMiddleware,
    allow_origins=["*"],
    allow_methods=["*"],
    allow_headers=["*"],
)

app.include_router(ingest.router, prefix="/ingest", tags=["ingest"])
app.include_router(clusters.router, prefix="/clusters", tags=["clusters"])
app.include_router(anomalies.router, prefix="/anomalies", tags=["anomalies"])
app.include_router(stats.router, prefix="/stats", tags=["stats"])
app.include_router(investigate.router, tags=["investigate"])
app.include_router(benchmark.router, prefix="/benchmark", tags=["benchmark"])
```

- [ ] **Step 2: Verify app starts without errors (mock external deps)**

Run: `cd services/aiops && OPENAI_API_KEY=test SQLITE_PATH=:memory: py -c "from app.main import app; print('OK')"`
Expected: OK (or ImportError to fix)

- [ ] **Step 3: Commit**

```bash
git add services/aiops/app/main.py
git commit -m "feat: FastAPI main app with lifespan, CORS, all routers"
```

---

## Task 14: Keep Setup Script

**Files:**
- Create: `scripts/setup_keep.py`

Depends on: Task 2 (Docker Compose)

- [ ] **Step 1: Implement `setup_keep.py`**

```python
"""One-time Keep configuration script.
Run after docker-compose up: py scripts/setup_keep.py
"""
import httpx
import time
import sys

KEEP_API_URL = "http://localhost:8080"
MAX_RETRIES = 30
RETRY_INTERVAL = 2


def wait_for_keep():
    for i in range(MAX_RETRIES):
        try:
            r = httpx.get(f"{KEEP_API_URL}/healthcheck")
            if r.status_code == 200:
                print("Keep API is ready")
                return
        except httpx.ConnectError:
            pass
        print(f"Waiting for Keep API... ({i+1}/{MAX_RETRIES})")
        time.sleep(RETRY_INTERVAL)
    print("Keep API not available")
    sys.exit(1)


def create_api_key():
    r = httpx.post(
        f"{KEEP_API_URL}/settings/apikey",
        json={"name": "aiops-service"},
        auth=("admin", "admin"),  # default Keep credentials
    )
    if r.status_code in (200, 201):
        key = r.json().get("apiKey", r.json().get("api_key", ""))
        print(f"API key created: {key[:8]}...")
        return key
    print(f"Failed to create API key: {r.status_code} {r.text}")
    return ""


def main():
    wait_for_keep()
    api_key = create_api_key()
    if api_key:
        print(f"\nAdd to .env:\nKEEP_API_KEY={api_key}")


if __name__ == "__main__":
    main()
```

- [ ] **Step 2: Commit**

```bash
git add scripts/setup_keep.py
git commit -m "feat: Keep setup script for API key creation"
```

---

## Task 15: Grafana Provisioning

**Files:**
- Create: `grafana/provisioning/datasources/datasources.yml`
- Create: `grafana/provisioning/dashboards/dashboards.yml`
- Create: `grafana/provisioning/dashboards/log-clusters.json`
- Create: `grafana/provisioning/dashboards/anomalies.json`
- Create: `grafana/provisioning/dashboards/incidents.json`
- Create: `grafana/provisioning/dashboards/ai-reports.json`
- Create: `grafana/provisioning/dashboards/benchmark.json`

Depends on: Task 10, Task 11 (API response schemas)

- [ ] **Step 1: Create `datasources.yml`**

```yaml
apiVersion: 1

datasources:
  - name: AIOps Service
    type: yesoreyeram-infinity-datasource
    access: proxy
    url: http://aiops-service:8081
    isDefault: true
    editable: true

  - name: Keep API
    type: yesoreyeram-infinity-datasource
    access: proxy
    url: http://keep-api:8080
    editable: true
```

- [ ] **Step 2: Create `dashboards.yml`**

```yaml
apiVersion: 1

providers:
  - name: AIOps
    type: file
    options:
      path: /etc/grafana/provisioning/dashboards
      foldersFromFilesStructure: false
```

- [ ] **Step 3: Create dashboard JSONs**

Create 5 Grafana dashboard JSON files using Infinity datasource panels:
- `log-clusters.json` -- table + time series from `/clusters`, `/clusters/timeline`
- `anomalies.json` -- stat panels + table from `/anomalies`, `/stats`
- `incidents.json` -- table from Keep `/incidents`
- `ai-reports.json` -- table from `/reports`
- `benchmark.json` -- stat panels from `/benchmark/results`

Each JSON follows Grafana dashboard model with Infinity datasource queries pointing to the REST endpoints.

- [ ] **Step 4: Verify Grafana provisioning files are valid JSON**

Run: `py -c "import json, glob; [json.load(open(f)) for f in glob.glob('grafana/provisioning/dashboards/*.json')]; print('All valid')"`
Expected: All valid

- [ ] **Step 5: Commit**

```bash
git add grafana/
git commit -m "feat: Grafana provisioning with 5 dashboards and Infinity datasources"
```

---

## Task 16: Integration Test

**Files:**
- Create: `services/aiops/tests/test_integration.py`

Depends on: All previous tasks

- [ ] **Step 1: Write end-to-end integration test**

File: `services/aiops/tests/test_integration.py`

```python
"""Integration test: runs the full pipeline with mock OpenAI and mock Keep."""
import json
import os
import tempfile
import pytest
from fastapi.testclient import TestClient
from unittest.mock import patch, MagicMock, AsyncMock


@pytest.fixture
def test_app(sample_openrca_dir):
    """Create test app with real Drain + SQLite, mocked OpenAI + Keep + ChromaDB."""
    os.environ["OPENAI_API_KEY"] = "test-key"
    os.environ["SQLITE_PATH"] = os.path.join(tempfile.mkdtemp(), "test.db")
    os.environ["OPENRCA_DATA_DIR"] = sample_openrca_dir
    os.environ["CHROMA_URL"] = "http://localhost:8000"
    os.environ["KEEP_API_URL"] = "http://localhost:8080"

    with patch("app.agent.rag.chromadb"), \
         patch("app.agent.rag.OpenAI"):
        from app.main import app
        yield TestClient(app)


def test_full_pipeline(test_app):
    # 1. Ingest OpenRCA data
    response = test_app.post("/ingest/openrca", json={
        "dataset": "Bank",
        "date": "2024_01_15"
    })
    assert response.status_code == 200

    # 2. Check clusters were created
    response = test_app.get("/clusters")
    assert response.status_code == 200
    clusters = response.json()
    assert len(clusters) > 0

    # 3. Check stats
    response = test_app.get("/stats")
    assert response.status_code == 200
    stats = response.json()
    assert stats["total_logs"] > 0

    # 4. Check anomalies
    response = test_app.get("/anomalies")
    assert response.status_code == 200
```

- [ ] **Step 2: Run integration test**

Run: `cd services/aiops && py -m pytest tests/test_integration.py -v`
Expected: PASS

- [ ] **Step 3: Commit**

```bash
git add services/aiops/tests/test_integration.py
git commit -m "test: end-to-end integration test for pipeline"
```

- [ ] **Step 4: Full test suite**

Run: `cd services/aiops && py -m pytest -v`
Expected: All tests PASS

- [ ] **Step 5: Final commit**

```bash
git add -A && git commit -m "chore: final cleanup, all tests passing"
```
